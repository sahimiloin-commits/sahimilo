/* global supabase, SAHIMILO_CONFIG */
const db=window.supabase?.createClient(SAHIMILO_CONFIG.supabaseUrl,SAHIMILO_CONFIG.supabasePublishableKey),$=id=>document.getElementById(id);let mode="login";
function message(text,ok=false){$("customerMessage").textContent=text;$("customerMessage").className="notice "+(ok?"notice-ok":"notice-error")}
function validGmail(email){return /^[^\s@]+@gmail\.com$/i.test(email)}
function validPin(pin){return /^\d{6}$/.test(pin)}
function refresh(){document.querySelectorAll(".account-tab").forEach(x=>x.classList.toggle("active",x.dataset.mode===mode));$("confirmPinField").classList.toggle("hidden",mode!=="signup");$("customerPin").autocomplete=mode==="signup"?"new-password":"current-password";$("customerHeading").textContent=mode==="signup"?"Create Customer Account":"Customer Login";$("customerIntro").textContent=mode==="signup"?"Gmail aur 6-digit PIN se apna SahiMilo account banayein.":"Apne Gmail aur SahiMilo PIN se login karein.";$("customerSubmit").textContent=mode==="signup"?"Create Account":"Login";$("customerMessage").classList.add("hidden")}
document.querySelectorAll(".account-tab").forEach(tab=>tab.onclick=()=>{mode=tab.dataset.mode;$("customerForm").reset();refresh()});
$("customerForm").onsubmit=async e=>{e.preventDefault();const email=$("customerEmail").value.trim().toLowerCase(),pin=$("customerPin").value,confirm=$("customerPinConfirm").value;if(!validGmail(email))return message("Valid @gmail.com address enter karein.");if(!validPin(pin))return message("PIN exactly 6 digits ka hona chahiye.");if(mode==="signup"&&pin!==confirm)return message("PIN aur Confirm PIN match nahi karte.");const button=$("customerSubmit");button.disabled=true;const result=mode==="signup"?await db.auth.signUp({email,password:pin,options:{data:{account_type:"customer"}}}):await db.auth.signInWithPassword({email,password:pin});button.disabled=false;if(result.error)return message(result.error.message);if(mode==="signup"&&!result.data.session)return message("Supabase mein Confirm email abhi ON hai. Use OFF karke phir account banayein.");location.replace("customer-dashboard.html")};
(async()=>{const {data:{session}}=await db.auth.getSession();if(session)location.replace("customer-dashboard.html")})();refresh();