/* global supabase, SAHIMILO_CONFIG */
const db=window.supabase?.createClient(SAHIMILO_CONFIG.supabaseUrl,SAHIMILO_CONFIG.supabasePublishableKey),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));let profile=null,user;
function msg(text,ok=false){$("profileMessage").textContent=text;$("profileMessage").className="notice "+(ok?"notice-ok":"notice-error")}
function digits(v){return String(v||"").replace(/\D/g,"")}
async function init(){const {data:{session}}=await db.auth.getSession();if(!session)return location.replace("professional-auth.html");user=session.user;$("profileEmail").value=user.email||"";const {data,error}=await db.from("professionals").select("*").eq("user_id",user.id).maybeSingle();if(error)return msg(error.message);profile=data||null;fill();$("pageLoading").classList.add("hidden");$("professionalApp").classList.remove("hidden");loadAssigned()}
function fill(){const p=profile||{};$("profileStatus").textContent=p.status||"Not submitted";$("profileStatus").className="status-badge status-"+(p.status||"pending");$("profileName").value=p.name||"";$("profileExperience").value=p.experience_years||0;$("profilePhone").value=p.phone||"";$("profileWhatsapp").value=p.whatsapp||"";$("profileCategory").value=p.category||"";$("profileServices").value=(p.services||[]).join(", ");$("profileArea").value=p.area||"";$("profilePincode").value=p.pincode||"";$("profileCity").value=p.city||"";$("profileState").value=p.state||"Bihar";$("profileBio").value=p.bio||""}
$("profileForm").onsubmit=async e=>{e.preventDefault();const changes={name:$("profileName").value.trim(),experience_years:Number($("profileExperience").value||0),phone:$("profilePhone").value.trim(),whatsapp:$("profileWhatsapp").value.trim(),category:$("profileCategory").value.trim(),services:$("profileServices").value.split(",").map(x=>x.trim()).filter(Boolean),area:$("profileArea").value.trim(),pincode:$("profilePincode").value.trim(),city:$("profileCity").value.trim(),state:$("profileState").value.trim(),bio:$("profileBio").value.trim()};if(digits(changes.phone).length<10)return msg("Valid mobile number enter karein.");if(!changes.category||!changes.services.length||!changes.area||!changes.city||!changes.state)return msg("Category, services aur complete service area enter karein.");if(!/^\d{6}$/.test(changes.pincode))return msg("Valid 6-digit pincode enter karein.");let data,error;if(profile)({data,error}=await db.from("professionals").update(changes).eq("id",profile.id).eq("user_id",user.id).select().single());else({data,error}=await db.from("professionals").insert({...changes,user_id:user.id,status:"pending",verified_mobile:false,verified_whatsapp:false,verified_by_admin:false}).select().single());if(error)return msg(error.message);profile=data;fill();msg(profile.status==="pending"?"Profile save ho gayi. Admin approval pending hai.":"Profile details update ho gayi.",true);loadAssigned()};
async function loadAssigned(){if(!profile){$("assignedHistory").innerHTML='<p class="account-note">Profile save karne ke baad assigned customer requests yahan dikhengi.</p>';return}const {data,error}=await db.from("service_requests").select("id,category,service,location,description,status,created_at").eq("professional_id",profile.id).order("created_at",{ascending:false});if(error){$("assignedHistory").innerHTML="<p>"+esc(error.message)+"</p>";return}$("assignedHistory").innerHTML=data?.length?data.map(r=>'<article class="history-card"><div class="history-top"><strong>'+esc(r.service)+'</strong><span class="status-badge">'+esc(r.status)+'</span></div><p>'+esc(r.location)+'</p><p>'+esc(r.description||"No details")+'</p></article>').join(""):'<p class="account-note">Abhi koi customer request assign nahi hui hai.</p>'}
$("logoutBtn").onclick=async()=>{await db.auth.signOut();location.replace("professional-auth.html")};init();